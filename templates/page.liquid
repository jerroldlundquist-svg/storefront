<section class="section" style="padding: 100px 20px;">
  <div class="shell" style="max-width: 800px; margin: 0 auto;">
    <h1 class="h1" style="font-size: clamp(28px, 4vw, 52px); line-height: 1.12; margin-bottom: 24px;">{{ page.title }}</h1>
    <div class="rte" style="line-height: 1.6; color: #1a1f2b;">
      {{ page.content }}
    </div>
  </div>
</section>
